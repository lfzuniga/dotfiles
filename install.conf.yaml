- defaults:
    link:
      create: true
      relink: true

- clean: []

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf
    ~/.bashrc: bashrc 
    ~/upgrade_act_MacOS: upgrade_act_MacOS

- shell:
